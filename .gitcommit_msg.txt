fix(工作台): 项目任务删除改为级联删除（含所有后代），杜绝孤儿引用

- server.js sectionDelete 对 ptask：读取该项目全部任务，按「上级任务标题」(父 tid) 构建 parent→子 映射，从被删任务的 tid 出发 DFS 收集其全部后代（子/孙/…），连同自身一并删除；其余同层/无关任务不受影响。保证删除后不再有任何记录的「上级任务标题」指向已删除 tid。
- 删除采用 Set 去重 + seen 防环，既不漏删也不多删。
- 前端 delGeneric 对 ptask 弹出「将删除该任务及其所有子任务（含子任务的子任务）」的二次确认。
- 验证：起本地 server + 直连 API 跑 e2e——新建 根(28)/子(29)/孙(30)/独立控制组，删除根后三者全删、独立项保留、无孤儿 parent 引用、清理后数量复原。PASS。
